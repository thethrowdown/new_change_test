#name: hello_cd

on: workflow_dispatch 

jobs:

 deploy:
    name: Deploy
    uses: thethrowdown/new_change_test2/.github/workflows/managed-cd.yaml@main
    secrets: inherit
    runs-on: ubuntu-latest
    steps:
      - name: ServiceNow Change
        uses: thethrowdown/new_change_test2/.github/workflows/meh.yaml@main
        with:
          job-name: 'Deploy'
